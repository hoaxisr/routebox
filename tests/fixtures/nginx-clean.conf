# configuration file /etc/nginx/nginx.conf:
http {
    include /etc/nginx/conf.d/*.conf;
}
# configuration file /etc/nginx/conf.d/blog.conf:
server {
    listen 80;
    server_name blog.example.com;
}
